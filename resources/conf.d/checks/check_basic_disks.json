{
  "checks": {
    "check_basic_disks": {
      "command": "TOKEN_SENSU_CLIENT_DIR/plugins/basic/check-disk.rb -d -w 75 -c 90 -x nfs -i /boot -i /dev/shm",
      "handlers": [
        "default"
      ],
      "subscribers": [
        "basic"
      ],
      "interval": 60
    }
  }
}
